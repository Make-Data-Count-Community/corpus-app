import React from 'react'
// import { lorem } from 'faker'

import { FilterButton } from '../../app/ui'

export const Base = () => <FilterButton>Filter Facets</FilterButton>

export default {
  component: FilterButton,
  title: 'Common/FilterButton',
}
