import React from 'react'

import { FilterHeader } from '../../app/ui'

export const Base = args => <FilterHeader {...args} />

export default {
  component: FilterHeader,
  title: 'Common/FilterHeader',
}
