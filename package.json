{
  "name": "app-template",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "repository": {
    "type": "git",
    "url": "git@gitlab.coko.foundation:project-templates/app-template.git"
  },
  "license": "MIT",
  "author": "",
  "main": "index.js",
  "scripts": {
    "clean": "sh clean.sh",
    "cz": "git cz",
    "prepare": "husky install",
    "storybook": "docker-compose -f docker-compose.storybook.yml up",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "config": {
    "commitizen": {
      "path": "cz-customizable"
    }
  },
  "devDependencies": {
    "@coko/lint": "^2.0.1"
  }
}
